{{- range .Values.services }}
{{- if and .enabled .redis.enabled }}
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ $.Chart.Name }}-{{ .name }}-redis
  namespace: {{ $.Values.namespace }}
  labels:
    app: {{ .name }}-redis
spec:
  replicas: 1
  selector:
    matchLabels:
      app: {{ .name }}-redis
  template:
    metadata:
      labels:
        app: {{ .name }}-redis
    spec:
      containers:
        - name: {{ .name }}
          image: {{ $.Values.redis.image }}
          ports:
            - containerPort: {{ $.Values.redis.port }}
              name: redis
          {{- with $.Values.redis.resources }}
          resources:
            {{- toYaml . | nindent 12 }}
          {{- end }}
---
apiVersion: v1
kind: Service
metadata:
  name: {{ $.Chart.Name }}-{{ .name }}-redis-service
  namespace: {{ $.Values.namespace }}
spec:
  selector:
    app: {{ .name }}-redis
  ports:
    - port: {{ $.Values.redis.port }}
      targetPort: redis
  type: ClusterIP
{{- end }}
{{- end }}
