{{- range $serviceName, $svc := .Values.services }}
{{- if $svc.enabled }}
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ $serviceName }}-config
data:
  config.yaml: |-
{{ $.Files.Get (printf "configs/%s.yaml" $serviceName) | indent 4 }}
{{- end }}
{{- end }}
